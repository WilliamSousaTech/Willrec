# Copie este arquivo para .env (uso local com `vercel dev`) e preencha as chaves.
# Em produção, configure essas mesmas variáveis em:
# Vercel → Settings → Environment Variables

# Google AI Studio → https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Groq Console (gratuito) → https://console.groq.com
GROQ_API_KEY=
